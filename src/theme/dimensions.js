import { MAX_DIMENSION } from './constants';


export default {
  full: `${MAX_DIMENSION}%`,
  half: `${MAX_DIMENSION * 0.5}%`,
  third: `${MAX_DIMENSION * 0.333}%`,
  twoThirds: `${MAX_DIMENSION * 0.666}%`,
  quarter: `${MAX_DIMENSION * 0.25}%`,
  fifth: `${MAX_DIMENSION * 0.20}%`,
  threeQuarters: `${MAX_DIMENSION * 0.75}%`,

  navBarHeight: `${3}rem`,

  padding: `${1}rem`,
  lgPadding: `${5}rem`,

  borderRadius: `${4}px`,
};
